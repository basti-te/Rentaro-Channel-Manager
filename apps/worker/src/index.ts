/**
 * @cm/worker — Inngest functions + webhook receivers.
 *
 * This service runs the Inngest dev server in development and the production
 * Inngest endpoint plus webhook handlers (Channex, Stripe, Twilio).
 *
 * Phase 5-6 will fill this in.
 */

function main() {
  console.log('Worker stub — see Phase 5 in docs/architecture.md');
}

main();
