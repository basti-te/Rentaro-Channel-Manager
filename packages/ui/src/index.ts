/**
 * @cm/ui — Shared React components built on Tailwind + Radix primitives.
 *
 * Phase 2 onward fills this in (Button, Sheet, Drawer, etc.).
 * Calendar lives in apps/web because it's app-specific, not reusable.
 */

export { cn } from './cn';
